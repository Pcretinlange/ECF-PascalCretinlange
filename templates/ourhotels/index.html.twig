{% extends 'base.html.twig' %}

{% block title %}Nos Hôtels{% endblock %}

{% block body %}
<main>
    <tbody>
    <article>
    <section class="row justify-content-center text-center">
            <div class="col-md-4">
                <h3 class="mt-5 mb-2">Hypnos Hotels</h3>
                <h4 class="trait-noir p-2">Découvrez nos Hôtels !</h4>
                <p class="mt-4">Depuis plus d’un siècle, Hypnos n’a jamais cessé de se réinventer, étant toujours plus à la pointe des dernières tendances et anticipant les nouveaux désirs de la clientèle. Aujourd'hui, le Groupe est fière de vous présenter ses hôtels, tous, plus prestigieux, ambitieux et fondés dans des écrins de luxe.
                </p>
                </div>
        </section>

        <section class="row justify-content-center ">{% for hotel in hotels %}
            <div class="seaview col-md-4 d-flex flex-column text-center p-4 align-items-center m-5">
                <h4>{{ hotel.name }}</h4>
                <h5 class="pt-1">{{ hotel.city }}</h5>
                <img class="card-img m-3" src="{{ 'img/hotel'~ hotel.id ~'.webp'}}" alt="{{ hotel.name }}">
                <p>{{ hotel.description }}</p>
                <button class="btn bg-dark text-white w-50">Réserver une suite</button>
            </div>
            {% endfor %}
        </section>
    </article>
        </tbody>

{% endblock %}
